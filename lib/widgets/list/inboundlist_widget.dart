import 'package:flutter/material.dart';

class InboundListWidget extends StatelessWidget {
  const InboundListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Inbound'),
    );
  }
}
